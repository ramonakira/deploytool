server {
    listen 127.0.0.1:%(nginx_port)s;

    client_max_body_size 5M;

    access_log %(log_path)s/nginx_access.log;
    error_log %(log_path)s/nginx_error.log;

    location /media/ {
        root %(current_instance_path)s/%(project_name)s;
        expires max;
    }

    location /static/ {
        root %(current_instance_path)s/%(project_name)s;
        expires max;
    }
}