{% block body %}
[unix_http_server]
file={{ vhost_path }}/supervisor/supervisor.sock

[supervisord]
nodaemon=true
logfile={{ log_path }}/supervisor.log
pidfile={{ vhost_path }}/supervisor/supervisor.pid
childlogdir={{ log_path }}

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl = unix://{{ vhost_path }}/supervisor/supervisor.sock

[program:gunicorn]
command={{ current_instance_path }}/env/bin/python manage.py run_gunicorn --access-logfile={{ log_path }}/gunicorn_access.log --error-logfile={{ log_path }}/gunicorn_error.log -b 127.0.0.1:{{ django_port }} -p {{ vhost_path }}/gunicorn.pid
directory={{ current_instance_path }}/{{ project_name }}
user={{ project_user }}
{% endblock %}